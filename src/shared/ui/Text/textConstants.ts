export enum TextTheme {
  PRIMARY = "primary",
  ERROR = "error",
}

export enum TextSize {
  M = "size-m",
  L = "size-l",
}

export enum AlignItems {
  RIGHT = "right",
  LEFT = "left",
  CENTER = "center",
}
